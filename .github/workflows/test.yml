name: Test

on: [push]

env:
  cache-version: v1

jobs:
  test:
    container:
      runs-on: self-hosted
      image: sximada/python-all
    steps:
      - uses: actions/checkout@v2
      - name: Run tox
        run: tox
